syntax = "proto3";

package scheduler;

service Scheduler {
    rpc RequestJob(JobRequest) returns (JobResponse);
}

message JobRequest {
    string runner_token = 1;
}

message JobResponse {
    optional JobInfo job = 1;
}

message JobInfo {
    int32 job_id = 1;
    string git_repository_url = 2;
}
