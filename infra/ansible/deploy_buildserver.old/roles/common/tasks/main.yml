---
- name: Create namespace
  kubernetes.core.k8s:
    api_version: v1
    kind: Namespace
    name: "{{ cluster_namespace }}"
    state: present

- name: Create ConfigMap
  kubernetes.core.k8s:
    state: "{{ k8s_state }}"
    definition:
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: buildserver-config
        namespace: "{{ cluster_namespace }}"
      data:
        BS_DATABASE_HOSTNAME: "{{ database_hostname }}"
        BS_DATABASE_PORT: "{{ database_port }}"
        BS_DATABASE_USER: "{{ database_user }}"
        BS_DATABASE_NAME: "{{ database_name }}"
        BS_RABBITMQ_HOST: "{{ rabbitmq_host }}"
        BS_RABBITMQ_PORT: "{{ rabbitmq_port }}"
        BS_RABBITMQ_USER: "{{ rabbitmq_user }}"

- name: Create Secret
  kubernetes.core.k8s:
    state: "{{ k8s_state }}"
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: buildserver-secrets
        namespace: "{{ cluster_namespace }}"
      type: Opaque
      data:
        BS_DATABASE_PASSWORD: "{{ database_password | b64encode }}"
        BS_RABBITMQ_PASSWORD: "{{ rabbitmq_password | b64encode }}"
